<div class="ui modal" id="modal">
  <i class="fas fa-times close"></i>
  <div class="ui header">
    <%= t('.edit') %>
  </div>
  
  <div class="content">
    <%= form_with model: @rule, as: :rule, url: admin_govern_rules_path(@govern) do |f| %>
      <%= render partial: 'form', locals: { f: f } %>
    <% end %>
  </div>
</div>
