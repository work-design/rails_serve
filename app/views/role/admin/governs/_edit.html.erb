<div class="ui modal" id="modal">
  <i class="close icon"></i>
  <div class="ui header">
    <%= t('.new') %>
  </div>
  
  <div class="content">
    <%= form_with model: @govern, as: :govern, url: admin_govern_path(@govern), local: true do |f| %>
      <%= render partial: 'form', locals: { f: f } %>
    <% end %>
  </div>
</div>
