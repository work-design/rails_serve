<tr>
  <td><%= name_space.name %></td>
  <td><%= name_space.identifier %></td>
  <td><%= name_space.verify_organ %></td>
  <td><%= name_space.verify_member %></td>
  <td><%= name_space.verify_user %></td>
  <td>
    <%= link_to({ action: 'show', id: name_space.id }, { aria: { label: t('.show') }, remote: true, class: 'button is-small is-rounded is-light' }) do %>
      <i class="fas fa-eye"></i>
    <% end %>
    <%= link_to({ action: 'edit', id: name_space.id }, { aria: { label: t('.edit') }, remote: true, class: 'button is-small is-rounded is-light' }) do %>
      <i class="fas fa-pencil-alt"></i>
    <% end %>
    <%= link_to({ action: 'destroy', id: name_space.id }, { method: :delete, aria: { label: t('.destroy') }, data: { confirm: t('.confirm') }, remote: true, class: 'button is-small is-rounded is-light' }) do %>
      <i class="fas fa-trash-alt"></i>
    <% end %>
  </td>
</tr>
