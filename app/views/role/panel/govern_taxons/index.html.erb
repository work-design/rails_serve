<%= render layout: 'index_tree_table', locals: { cache_key: GovernTaxon.column_names.hash } do %>
  <%= render partial: 'index_tbody', collection: @govern_taxons, as: :govern_taxon %>
<% end %>

<%= paginate @govern_taxons %>
