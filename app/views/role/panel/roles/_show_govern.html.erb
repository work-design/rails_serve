<tr id="tr_<%= govern.identifier %>" data-controller="tree_remote" data-tree_remote-target="item">
  <td>
    <turbo-frame>
      <span style="width: 2.86em; display: inline-block"></span>
      <%= link_to({ action: 'rules', govern_id: govern.id }) do %>
        <i class="fas fa-fw fa-caret-right" style="cursor: pointer" data-action="click->tree_remote#expand"></i>
      <% end %>
      <% if @role.has_role?(business: params[:business_identifier], namespace: params[:namespace_identifier], controller: govern.controller_name) %>
        <%= button_to({ action: 'govern_off' }, params: { govern_id: govern.id }, method: :patch) do %>
          <i class="far fa-check-square fa-lg"></i>
        <% end %>
      <% else %>
        <%= button_to({ action: 'govern_on' }, params: { govern_id: govern.id }, method: :patch) do %>
          <i class="far fa-square fa-lg"></i>
        <% end %>
      <% end %>
      <span><%= govern.name %></span>
    </turbo-frame>
  </td>
</tr>
