<%= turbo_stream.replace "tr_#{@meta_action.identifier}", partial: 'show_action', locals: { meta_action: @meta_action } %>
<%= turbo_stream.replace "tr_#{@meta_action.meta_controller.identifier}", partial: 'show_controller', locals: { meta_controller: @meta_action.meta_controller, expand: true } %>
<%=
  turbo_stream.replace(
    "tr_#{@meta_action.meta_namespace.id_by_business(@meta_action.business_identifier)}",
    partial: 'show_namespace',
    locals: { meta_namespace: @meta_action.meta_namespace, business_identifier: @meta_action.business_identifier, expand: true }
  )
%>
<%= turbo_stream.replace "tr_#{@meta_action.business_identifier}", partial: 'show_business', locals: { meta_business: @meta_action.meta_business, expand: true } %>
