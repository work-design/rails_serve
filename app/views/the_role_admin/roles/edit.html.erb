<div class="sixteen wide column">
  <div class="ui breadcrumb">
    <%= link_to 'Back', admin_roles_path, class: 'govern' %>
    <div class="divider"> / </div>
    <div class="active govern">Edit</div>
  </div>

  <div class="ui segment">
    <%= default_form_for @role, as: :role, url: admin_role_path(@role) do |f| %>
      <%= render partial: 'form', locals: { f: f } %>
    <% end %>
  </div>
</div>