<%= f.text_field :name, label: '名称:' %>
<%= f.collection_select :role_ids, Role.all, :id, :name, { label: 'Roles：' }, { multiple: true, class: 'ui fluid dropdown nine wide field' } %>
<%= f.submit %>

<script>
  $('.ui.dropdown.search').dropdown({});
</script>